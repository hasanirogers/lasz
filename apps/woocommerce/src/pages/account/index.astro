---
import Account from "../../components/Account.astro";
import Redirect from "../../components/Redirect.astro";
import Cookies from 'js-cookie';

const isLoggedIn = !!Cookies.get('lasz-user')
---

{isLoggedIn ? <Account /> : <Redirect to="/login" />}
